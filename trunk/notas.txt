Notas sobre version de Osezno-Framework

- Problemas encontrados
  Una consulta llamando dos veces a la misma tabla en el metodo order by genera
  problema ya que dice que ese campo es ambiguo
  
  Incompatibilidad en php 5.2.9-1, este error debe ser cacheado para asegurar la conpatibilidad
  Php 5.2 solo funciona sobre Apache 2.2
  Error
  Normalmente seria algo como 

Arreglar:  Revision, los errores de consultas manuales generadas por medio del metodo query pueden ser seguidas de cerca sobre errores de jecucion y po lo tanto deben abortar las transacciones q sean sus mentoras

Modificar el metodo query para obtener el numero de registros afectados por la anterior
consulta, este resultado no debe depender se si se trata un SELECT, UPDATE, INSERT o DELETE

(ERROR) Modificar en la clase de listas dinamicas cuando se aplica una longitud de 80 pixeles a una longitud total de la tabla de 1100 pixeles

Crear atributo de base de datos que permita deicidir si se aplican comillas automaticas cuando se trata de un variable char

Revizar como se comporta setAutoQuotesInFind en active record 