Al asignar un evento a un checkbox con el metodo addCheck de la clase OPF_myForm este evento tambien deberia producirse cuando el usuario hace click en la etiqueta del campo.
 
Cuando se agrega un evento a un checbox pero despues el campo es desabilitado con el metodo addDisabled este evento no se debe ejecutar.